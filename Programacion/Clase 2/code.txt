#include <stdio.h>
#include <stdlib.h>
#define CANTIDAD_NUMEROS 3
int obtenerNumeros (void);
int main()
{
    obtenerNumeros();
    return 0;
}

int obtenerNumeros(void){
    int numero;
    int acumulador=0;
    int i=0;
    float promedio;

    for(i;i<CANTIDAD_NUMEROS;i++)
    {
        printf("Ingrese un numero\n");
        scanf("%d",&numero);
        acumulador+=numero;
    }
    //promedio=(float)acumulador/CANTIDAD_NUMEROS;
    
    if(!calcularPromedio(acumulador,CANTIDAD_NUMEROS,&promedio))//negar es lo mismo que decir calcularPromedio()==0
    {
            printf("\nEl promedio es: %.2f",promedio);
    }
    else{
        printf("ERROR");
    }
    return 0;
}

int calcularPromedio(int valorAcumulado, int cantidad, float *promedio)
{
    int retorno=-1;
    if(cantidad>0 && promedio!=NULL)
    {
        *promedio=(float)valorAcumulado/cantidad;
        retorno=0;
    }
    return retorno;
}

int calcularMaximo (void)
{
    int maximo=0;
    int numero;
    int i=0;

    printf("Calcular el numero maximo");
    for(i;i<CANTIDAD_NUMEROS;i++)
    {
    printf("\nIngrese un numero\n");
    scanf("%d",&numero);
        if(numero>maximo){
            maximo=numero;
        }
    }
    printf("El numero maximo es: %d\n",maximo);
    return 0;
}

int calcularMinimo (void)
{
    int minimo=100;
    int numero;
    int i=0;

    printf("\nCalcular el numero minimo");
    for(i;i<CANTIDAD_NUMEROS;i++)
    {
    printf("\nIngrese un numero\n");
    scanf("%d",&numero);
        if(numero<minimo){
            minimo=numero;
        }
    }
    printf("El numero minimo es: %d\n",minimo);
    return 0;
}